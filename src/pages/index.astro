---
import HabitCard from "../components/HabitCard.astro";
import Layout from "../layouts/Layout.astro";
---

<Layout title="Habit Tracker">
  {[...Array(10)].map((card) => <HabitCard title="A habit" />)}
</Layout>
