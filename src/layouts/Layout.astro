---
export interface Props {
  title: string;
}

const { title } = Astro.props;
---

<!DOCTYPE html>
<html lang="en" data-theme="dark">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>
  </head>
  <body class="flex h-screen w-screen flex-col items-center bg-base-100">
    <h1 class="self-start text-3xl p-3 font-black">Habit Tracker</h1>
    <main
      class="grid w-full max-w-3xl grid-cols-2 gap-3 p-4 md:grid-cols-3 lg:grid-cols-4"
    >
      <slot />
    </main>
  </body>
</html>
